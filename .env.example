# Copy this file to .env for local development.
# The default SQLite database is enough for running the demo locally.
DATABASE_URL=sqlite:///./shoppinglist.db

# Change this for any non-local environment.
SECRET_KEY=change-me-in-production

# Set to 1 only when debugging locally.
DEBUG_ERRORS=0
